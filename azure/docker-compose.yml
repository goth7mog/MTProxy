version: '3.8'
services:
  mtproxy:
    build: .
    container_name: mtproxy
    ports:
      - "8388:8388/tcp"
      - "8388:8388/udp"
    env_file:
      - ../digitalocean/.env
    volumes:
      - ./dnscrypt-proxy.toml:/etc/dnscrypt-proxy.toml
